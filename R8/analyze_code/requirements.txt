# R8 analyze_code dependencies
numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
scipy>=1.11
zxing-cpp>=2.2

# GPU 加速（差分・統計・FFT）。CUDA 版は https://pytorch.org の指示に従うこと。
# 例 (CUDA 12.4): pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.1
